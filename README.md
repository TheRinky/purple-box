# Purple Box

RuneLite raid-purple celebration with a mystery-box-style item roll, purple glow,
slowing carousel, final reward reveal, and original procedural jingle.

## Test

Enable the plugin and enter `::purplebox` in chat. It recognizes announced uniques
from CoX, ToB, and ToA. If an announcement changes, configure **Extra trigger text**.

Build with `gradle build`.
